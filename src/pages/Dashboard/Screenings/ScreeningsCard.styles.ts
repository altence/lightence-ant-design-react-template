import styled from 'styled-components';
import { Card as StyledCard } from '../../../components/common/Card/Card';

export const Card = styled(StyledCard)`
  & .ant-card-body {
    position: relative;
  }
`;
