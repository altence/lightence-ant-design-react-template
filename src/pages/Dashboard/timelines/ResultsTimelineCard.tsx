import React from 'react';
import { Card } from '../../../components/common/Card';

export const ResultsTimelineCard: React.FC = () => {
  return <Card title="Patient timeline">Results timeline card</Card>;
};
